<h4>Issues</h4>
<table class="table table-striped">
  <tbody>
    {{#each}}
      <tr>
        <td style="width:120px;"><img {{bind-attr src=user.avatar_url}} style="width:100px;" /></td>
        <td>
          <b>{{title}}</b>
          <hr/>
          <p class="text-muted">{{from-date created_at}}</p>
        </td>
      </tr>
    {{else}}
      <tr>
        <td><p class="text-muted">No issues yet!</p>
      </tr>
    {{/each}}
  </tbody>
</table>

